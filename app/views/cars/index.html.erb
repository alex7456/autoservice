<div class="container mt-4">
  <h2 class="text-center mb-4">Список автомобилей</h2>

  <table class="table table-striped">
    <thead class="table-dark">
    <tr>
      <th>#</th>
      <th>Номер</th>
      <th>Марка</th>
      <th>Модель</th>
      <th>Год</th>
      <th>Действия</th>
    </tr>
    </thead>
    <tbody>
    <% @cars.each_with_index do |car, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= car.license_plate %></td>
        <td><%= car.brand %></td>
        <td><%= car.model %></td>
        <td><%= car.year %></td>
        <td>
          <a href="<%= edit_car_path(car) %>" class="btn btn-warning btn-sm">✏ Редактировать</a>
          <%= button_to car_path(car), method: :delete, data: { turbo_confirm: "Вы уверены?" }, class: "btn btn-danger btn-sm" do %>
            🗑 Удалить
          <% end %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <a href="<%= new_car_path %>" class="btn btn-success">➕ Добавить автомобиль</a>
</div>
