<div class="container mt-4">
  <h2 class="text-center mb-4">Список клиентов</h2>

  <table class="table table-striped">
    <thead class="table-dark">
    <tr>
      <th>#</th>
      <th>Имя</th>
      <th>Телефон</th>
      <th>Email</th>
      <th>Действия</th>
    </tr>
    </thead>
    <tbody>
    <% @customers.each_with_index do |customer, index| %>
      <tr>
        <td><%= index + 1 %></td> <!-- Порядковый номер -->
        <td><%= customer.name %></td>
        <td><%= customer.phone %></td>
        <td><%= customer.email %></td>
        <td>
          <a href="<%= edit_customer_path(customer) %>" class="btn btn-warning btn-sm">✏ Редактировать</a>
          <%= button_to customer_path(customer), method: :delete, data: { turbo_confirm: "Вы уверены?" }, class: "btn btn-danger btn-sm" do %>
            🗑 Удалить
          <% end %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <a href="<%= new_customer_path %>" class="btn btn-success">➕ Добавить клиента</a>
</div>
